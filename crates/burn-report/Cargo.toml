[package]
authors = ["Burn Contributors"]
categories = ["science"]
description = "Reporting and visualization tools for Burn training (TensorBoard support)"
edition.workspace = true
keywords = ["deep-learning", "machine-learning", "tensorboard", "reporting"]
license.workspace = true
name = "burn-report"
readme = "README.md"
repository = "https://github.com/tracel-ai/burn/tree/main/crates/burn-report"
documentation = "https://docs.rs/burn-report"
version.workspace = true

[lints]
workspace = true

[dependencies]
burn-core = { path = "../burn-core", version = "=0.21.0", default-features = false }
burn-train = { path = "../burn-train", version = "=0.21.0", default-features = false }

# Utilities
parking_lot = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
